
<div class="mt-4">
        <h1>User: {{email}}</h1>
    <h3>Đăng ký user</h3>
     {{!-- action="/users/store --}}
    <form method="POST" name="form" action="/login/register">
        <div class="form-group">
            <label for="email">Email</label>
            <input type="text" class="form-control" id="name" name="email">
        </div>
        <div class="form-group">
            <label for="passwordHash">Mật khẩu</label>
            <input type="text" class="form-control" id="password" name="passwordHash">
        </div>
        
        <button type="submit" class="btn btn-primary">Đăng ký</button>
        
    </form>
</div>
<div class="mt-4">
    <h3>Đăng Nhập user</h3>
     {{!-- action="/users/store --}}
    <form method="POST" name="form" action="/login/verifyLogin">
        <div class="form-group">
            <label for="email">Email</label>
            <input type="text" class="form-control" id="email" name="email">
        </div>
        <div class="form-group">
            <label for="passwordHash">Mật khẩu</label>
            <input type="text" class="form-control" id="passwordHash" name="passwordHash">
        </div>
        
        <button type="submit" class="btn btn-primary">Đăng Nhập</button>
        
    </form>
</div>
<div class="mt-4">
    <h3>Đăng xuất user</h3>
     {{!-- action="/users/store --}}
    <form method="POST" name="form" action="/login/logout">
        <button type="submit" class="btn btn-primary">Đăng xuất</button>
    </form>
</div>
<br>
<p>
    Cần phải đăng nhập mới có thể xem danh sách
</p>
<form method="GET" name="form" action="/login/studentList">
        <button type="submit" class="btn btn-primary">Hiện danh sách users</button>
</form>
{{!-- <div class="mt-4">
    <table class="table mt-4">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">id</th>
                <th scope="col">Tên đăng nhập</th>
                <th scope="col">Password</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
            <tr>
                <th scope="row">{{sum @index 1}}</th>
                <td>{{this._id}}</td>
                <td>{{this.email}}</td>
                <td>{{this.passwordHash}}</td>
            </tr>
            {{else}}
            <tr>
              <td colspan="5" class="text-center">
                Chưa có người dùng nào.
              </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div> --}}